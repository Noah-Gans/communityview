# Tour scroll-scrub frames

**351 WebP frames** at **1920×1080**, **20fps** (~76MB total).

Re-export:

```bash
./scripts/marketing/export-tour-frames.sh
```

Update `frameCount` in `src/pages/landingPages/content/tourScrub.js` if the count changes.
